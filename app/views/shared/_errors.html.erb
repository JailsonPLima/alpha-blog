<% if obj.errors.any? %>
    <h1>The following errors prevented the <%= obj.class.name.downcase %> from being saved</h1>
    <ul>
        <% obj.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
    </ul>
<% end %>